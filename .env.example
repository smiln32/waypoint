# Copy to .env.local and set your key to enable real AI critiques.
# Without a key, the editors fall back to grounded demo findings.
ANTHROPIC_API_KEY=
# Optional model override (default: claude-opus-4-8)
ANTHROPIC_MODEL=
