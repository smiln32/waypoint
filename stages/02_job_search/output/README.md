# Output — per-run working artifacts

Files here are Layer 4: they change per run, are human-editable, and are safe to edit or delete.
Convention: `<YYYY-MM-DD-HHmmss>-critique.json` for AI critique runs (see `_config/shared/finding-format.md`).
The app writes here best-effort in development only; a missing write never fails a request.
