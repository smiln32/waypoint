# Output location

Actual Stage 06 runs belong in `output/runtime/`, which is ignored by Git because runtime records may contain private career information. Only fictional samples belong in `examples/`.
